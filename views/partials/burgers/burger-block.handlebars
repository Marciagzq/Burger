    <li>
        {{burger_name}}
        <button class="devoured-class" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Save{{else}}Devoured{{/if}}
	</button> 

	<button class="delete-cat" data-id="{{this.id}}">X</button>

    </li>

       <style>
     

    </style>